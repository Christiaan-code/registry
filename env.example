# Docker Registry Configuration
# This file is for your reference only - the actual authentication
# is handled by the auth/htpasswd file generated during setup

# Registry URL (update this with your Cloudflare tunnel URL after setup)
REGISTRY_URL=localhost:5000

# Note: Your registry credentials are stored in auth/htpasswd
# To add more users, run:
#   docker run --rm --entrypoint htpasswd httpd:2 -Bbn username password >> auth/htpasswd

# Optional: Cloudflare tunnel configuration
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here
